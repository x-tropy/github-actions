name: 🚀 first-workflow

on: push

jobs: 
  a-job-rocks:
    name: '🔥 A job that rocks'
    runs-on: ubuntu-latest
    steps:
      - name: log the variables
        env:
          var1: This is
          var2: a test
        run: |
          echo $var1
          echo $var2.

