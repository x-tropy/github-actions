name: first-workflow

on: push

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: log the variables
        env:
          var1: This is
          var2: a test
        run: |
          echo $var1
          echo $var2.

